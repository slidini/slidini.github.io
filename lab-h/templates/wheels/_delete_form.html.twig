<form method="post" action="{{ path('app_wheels_delete', {'id': wheel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wheel.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
